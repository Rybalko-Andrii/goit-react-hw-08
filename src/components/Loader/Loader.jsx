import React from "react";

const Loader = () => (
  <div className="relative w-screen h-screen">
    <span className="loading loading-spinner loading-lg absolute " />
  </div>
);

export default Loader;
